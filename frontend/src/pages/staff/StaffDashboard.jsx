import { PageShell } from '../_PageShell';
export default function StaffDashboard() {
  return <PageShell title="Dashboard" roleLabel="Staff" path="/staff/dashboard"
    description="Pending document upload tasks, recent uploads, and today's patient list for document support operations." />;
}
