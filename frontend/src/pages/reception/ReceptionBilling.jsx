import { PageShell } from '../_PageShell';
export default function ReceptionBilling() {
  return <PageShell title="Billing" roleLabel="Receptionist" path="/reception/billing"
    description="Create invoices, record payments (cash, card, UPI, insurance), issue receipts, and manage pending payment follow-up." />;
}
