import { PageShell } from '../_PageShell';
export default function ReceptionAppointments() {
  return <PageShell title="Appointments" roleLabel="Receptionist" path="/reception/appointments"
    description="Book, reschedule, or cancel appointments. View available doctor slots, manage waitlist entries, and send WhatsApp confirmations." />;
}
