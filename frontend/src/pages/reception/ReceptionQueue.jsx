import { PageShell } from '../_PageShell';
export default function ReceptionQueue() {
  return <PageShell title="Queue" roleLabel="Receptionist" path="/reception/queue"
    description="Real-time token queue for the current clinic session. Check patients in, see who is with the doctor, and manage waiting order." />;
}
