import { PageShell } from '../_PageShell';
export default function AdminWaitlist() {
  return <PageShell title="Waitlist" roleLabel="Administrator" path="/admin/waitlist"
    description="Active waitlist entries across all doctors and branches. Offer slots, mark accepted or expired, monitor bypass events." />;
}
