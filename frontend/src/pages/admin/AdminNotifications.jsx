import { PageShell } from '../_PageShell';
export default function AdminNotifications() {
  return <PageShell title="Notifications" roleLabel="Administrator" path="/admin/notifications"
    description="WhatsApp automation log, failed notification queue with retry controls, and N8N workflow health dashboard." />;
}
