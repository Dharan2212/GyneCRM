import { PageShell } from '../_PageShell';
export default function AdminSettings() {
  return <PageShell title="Settings" roleLabel="Administrator" path="/admin/settings"
    description="Hospital profile, branch configuration, service catalog, appointment types, lab test catalog, and protocol templates." />;
}
